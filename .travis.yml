sudo: required
addons:
  chrome: stable
language: node_js
node_js:
  - "12.0"
branches:
  only:
    - master
before_script:
  - npm install -g @angular/cli
script:
  - cd redfox-sport-ui
  - npm install
  - ng build --prod
  - npm run lint
  - npm run test
deploy:
  on:
    branch: master
  skip_cleanup: true
  provider: firebase
  token:
    secure: $FIREBASE_TOKEN